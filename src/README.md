# src

Sherlock source package root.

Sub-packages:
- `orchestrator/` — workflow routing and intent triage
- `agents/`       — individual LLM agent implementations
- `memory/`       — progressive summarization & long-term memory
- `pipelines/`    — end-to-end task pipelines
- `tools/`        — shared utilities and external integrations
